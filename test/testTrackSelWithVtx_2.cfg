process Test2 = {
    include "FWCore/MessageService/data/MessageLogger.cfi"

    source = PoolSource {
        untracked vstring fileNames = { 'file:tracks.root' }
    }

    include "PhysicsTools/RecoSelectors/data/TrackWithVertexSelector.cfi"

    module out = PoolOutputModule {
        untracked string fileName = 'select-vtx.root' 
        untracked vstring outputCommands = {
                'drop *',
                'keep recoTracks_*_*_*'
        }
    }

    module ascii = AsciiOutputModule { }

    path    p = { trackWithVertexSelector }

    replace trackWithVertexSelector.useVtx    = true
    replace trackWithVertexSelector.nVertices = 2

    endpath e = { ascii, out }
}
